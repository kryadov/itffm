# TODO / Идеи

Копилка того, что впереди. Сделанное живёт в тегах и релизах (v0.1.0 … текущий),
здесь — что дальше. Порядок внутри разделов: сначала то, что чинит уже
обещанное, потом новое.

Планы с конкретикой лежат отдельно: [`docs/superpowers/plans`](docs/superpowers/plans/),
обоснования решений — в [`docs/superpowers/specs`](docs/superpowers/specs/).

---

## 🐞 Известные шероховатости

- [ ] **Земля — ровный зелёный ковёр.** Подстилки, травы и цветов всё ещё нет
      (валеж и валуны уже есть — см. «Наполнение леса»), и из-за этого лес
      выглядит стерильно местами. Мешает и честному поиску: гриб либо видно,
      либо нет, без «выглядывает из-под листьев».
- [ ] **Деревья не отбрасывают тени.** Формы крон разведены (три силуэта на
      лиственных, v0.5.0), а вот `HemisphereLight` + `DirectionalLight` в
      `game/scene.ts` теней не дают вовсе, и лес выглядит плоско освещённым в
      любую погоду. Полторы тысячи деревьев с честными тенями — уже вопрос
      производительности, а не только кода: разбираться вместе с LOD и
      инстансингом деревьев (см. «Внутреннее»), а не просто включить
      `castShadow` в лоб.

## 🚧 Ближайшее

- [ ] **ETL и фотографии** — план 3. Таксономия из GBIF, названия из Wikidata,
      фото из iNaturalist Open Data с лицензией и автором. Поле `media` в схеме
      уже есть и валидируется, данные лягут в готовое место. Заодно проверить и
      при необходимости заменить `gbifKey` во всех 25 видах — сейчас это
      правдоподобные заглушки, расставленные вручную при курации, а не сверенные
      с настоящим GBIF Backbone.
- [ ] **Фильтры в энциклопедии** — по биому, сезону, съедобности, типу
      гименофора. Сейчас это просто сетка.
- [ ] **Размер участка — настраиваемый, в километрах.** `HALF_SIZE` в
      `game/scene.ts` сейчас константа (90 м), от которой зависят и радиус
      OSM-запроса (`OSM_RADIUS` в `game/loadForest.ts`), и частота сетки земли
      (`GROUND_SEGMENTS`), и плотность спавна грибов. Нужно вынести в настройки:
      больше участок — дольше грузится и тяжелее для GPU, но реалистичнее
      ощущается «настоящий» лес, а не вырезанный из него квадрат.

## 🌤 Атмосфера и жизнь леса

Почти всё это уже написано и обкатано в
[`race-the-city`](https://github.com/kryadov/race-the-city) — брать оттуда, а не
изобретать. Пути ниже указаны относительно того репозитория.

- [ ] **Трава.** Единственное в списке, чего у соседей нет в готовом виде:
      `world/ground.ts` там красит землю, а травинок не рисует. Нам она нужна
      не для красоты — гриб должен *выглядывать* из травы, а не лежать на
      бильярдном сукне, иначе поиск вырождается в «видно или не видно».
      Инстансированные кресты с alpha, гуще во влажных низинах (влажность уже
      считается в `ecology/sites.ts`), прореженные по дальности.
- [ ] **Птицы** — `src/app/birds.ts` (888 строк, готовое). Стая садится на
      кроны: принимает список `TreePerch` — точек в кронах, — который у нас
      выдаст `world/trees.ts`. Для тихой игры про лес птицы дают больше
      присутствия, чем любая графика.
- [ ] **Небо, солнце и луна** — `src/app/sky.ts`. Купол с градиентом, диском
      солнца и процедурными звёздами, один вызов отрисовки. Звёзды хешируются
      из направления взгляда, а не существуют объектами.
- [ ] **Смена дня и ночи** — `src/app/daynight.ts`. Ключевые кадры полночь →
      рассвет → полдень → закат, режимы `cycle` / `day` / `night`. Ложится на
      наш будущий календарь: сезон и время суток — одна и та же ось.
- [ ] **Динамичные облака** — `src/app/clouds.ts` (87 строк). Высоты считаются
      от земли, а не от уровня моря — важно, когда дойдём до высокогорья.
- [ ] **Погода** — `src/app/weather.ts`. Дождь, снег, туман частицами в коробке
      вокруг камеры. У нас `daysSinceRain` уже влияет на спавн, но игрок дождя
      не видит и планировать по нему не может.
- [ ] **Фонарик на `F`.** Своё, но простое: `THREE.SpotLight`, привязанный к
      камере. Осмысленным станет вместе с ночью — до неё ставить нечего.
- [ ] **Тропинки.** В OSM это `highway=path|footway|track`, и у соседей они уже
      разбираются: `src/geo/parse.ts` сводит `footway|path|pedestrian|cycleway`
      в тип `'path'`, а `src/world/roads.ts` их рисует. Тропа — не декорация:
      по ней игрок входит в лес и по ней же находит дорогу назад.
- [ ] **Водоёмы** — `src/world/water.ts`, `waterArea.ts`, `sea.ts`. Там решены
      неочевидные вещи: юбка по кромке, чтобы под водой не было видно дневного
      света на склоне берега, и посадка уреза воды на грубый DEM. Нам вода
      нужна и как ориентир, и как источник влажности — `ecology/sites.ts` уже
      готов её учитывать.
- [ ] **Меню настроек** — `src/ui/menu.ts` + `src/app/prefs.ts` (сохранение
      выбранного в `localStorage`). Первым делом туда: скорость ходьбы, язык,
      время суток, погода, чувствительность мыши, дальность прорисовки.

## 🚶 Ходьба и ориентирование

- [ ] **Покачивание камеры и ритм шага.** Сейчас игрок скользит по лесу, как на
      тележке. Небольшой боковой качок в такт шагу — и это же даёт момент, к
      которому цепляются звуки шагов из раздела выше: один источник ритма, а не
      два независимых.
- [ ] **Мини-карта** — `src/ui/minimap.ts` (266 строк). Вращающаяся, рисует мир
      один раз в offscreen-канву, показывает цель на ободе, когда та за краем.
      **Но:** карта, показывающая твоё положение, убивает половину удовольствия
      от грибной прогулки — потерять и снова найти тропу это и есть занятие.
      Поэтому: компас по умолчанию, мини-карта пунктом в настройках, и точно
      без отметок грибов на ней.

## 🔊 Звук

Тон игры держится на тишине, и тишина должна быть живой. Из race-the-city
переносится только обвязка — микшер, громкости, пункт в меню (`src/audio/audio.ts`,
498 строк); содержимое там своё, мотор и радио, нам оно не подходит.

- [ ] **Фон леса** — ветер в кронах, скрип стволов, шорох листвы. Меняется от
      погоды и времени суток, а не крутится одной петлёй.
- [ ] **Шаги** — разные по субстрату: подстилка, мох, песок, вода. Субстрат в
      точке уже известен (`ecology/sites.ts`), брать оттуда. Ритм берётся из
      покачивания камеры, см. раздел выше.
- [ ] **Вода** — речка и озеро слышны раньше, чем видны, и громче вблизи. Это
      даёт настоящий ориентир в лесу, где ориентиров мало.
- [ ] **Птицы** — привязать к стае из `birds.ts`, чтобы голос шёл из точки, где
      птица сидит, а не из ниоткуда.
- [ ] **Сбор гриба** — короткий сухой звук среза. Единственное «событие» в игре,
      и оно должно ощущаться.
- [ ] Источник записей: CC0-библиотеки (freesound под CC0, BBC Sound Effects).
      Лицензию каждого файла хранить рядом, как у фотографий видов.

## 🌲 Наполнение леса

Кусты, валежник и камни — **не декорация, а субстрат**. Валеж (`deadwood`) и
валуны (`moss`) уже стоят в мире и дают реальную опору грибам этих субстратов
(v0.5.0); `litter`, `livewood`, `burnt` пока представлены только случайной
почвой без своей геометрии.

- [ ] **Ягодные кусты** — черника, брусника, малина. Просятся собираться так же,
      как грибы, и это соблазн, которому стоит поддаться: ягоды растут в других
      местах, чем грибы, и заставляют смотреть под ноги иначе. Но сперва
      решить, идут ли они в ту же энциклопедию или в отдельную.
- [ ] **Гриб прячется в траве и под кроной, а не просто стоит среди неё.**
      Пункты выше про траву и подлесок дают шум на глаз, но сейчас это не
      влияет на саму механику поиска: `nearestInView` (`game/pick.ts`) видит
      гриб, как только луч из центра экрана попадает в его меш, и трава этому
      никак не мешает — как в настоящем лесу, где низкий гриб под папоротником
      или в тени плотной кроны действительно трудно заметить. Нужен эффект
      скрытности: либо трава и подлесок физически перекрывают луч (тогда это
      само собой получается из геометрии, без отдельного кода), либо у
      размещения (`ecology/spawn.ts`) появляется поле «скрытость» от близости
      высокой травы/кроны, влияющее на дистанцию, с которой подсказка вообще
      появляется. Второе честнее для геймплея, но требует нового поля в
      `Placement`.
- [ ] Когда дойдём до реальной географии, домик-землянку брать из OSM, а не
      выдумывать: `tourism=wilderness_hut`, `amenity=shelter`, `building=hut`,
      `man_made=tower` (вышка) — всё это в лесах размечено. Пока что она одна
      на лес и ставится процедурно (`world/shelter.ts`).
- [ ] Всё перечисленное — инстансами и с отсечением по дальности с самого
      начала. Полторы тысячи деревьев уже стоят, бюджет не резиновый.

## 📱 Мобильные устройства

Отдельным этапом, после реальной географии. Сейчас игра неиграбельна на
телефоне: управление построено на клавиатуре и захвате указателя, которых там
нет.

- [ ] **Тач-управление** — виртуальный стик для ходьбы, свайп для обзора, тап по
      грибу вместо прицела с `E`. Захват указателя не применяется, значит вся
      логика `controls.ts` разветвляется по типу ввода. Основа есть готовая:
      `src/ui/touchControls.ts` в race-the-city.
- [ ] **Экранные кнопки** вместо `Tab` и `Q`: энциклопедия, корзина, приседание.
- [ ] **Портретная раскладка** для энциклопедии и карточки вида — сейчас осмотр
      это две колонки, на телефоне нужны две вкладки.
- [ ] **Бюджет производительности.** 243 гриба и полторы тысячи деревьев тянет
      десктоп; на телефоне нужны более агрессивное отсечение по дальности,
      меньше сегментов в телах вращения и, вероятно, инстансинг грибов одного
      вида. Мерить на реальном устройстве, а не на эмуляции.
- [ ] **PWA** — офлайн-запуск и иконка на домашний экран. Без бэкенда это
      дёшево: манифест плюс сервис-воркер на статику.

## 🌍 Биомы за пределами леса

Все девять биомов схемы теперь заселены — по каждому есть хотя бы один вид
(дюны: *Psathyrella ammophila*; болото: *Russula emetica*; высокогорье:
*Leccinum scabrum*, *Hygrocybe conica*; штольни: *Pleurotus ostreatus*,
*Coprinellus micaceus*; опушки: *Agaricus campestris*, *Coprinus comatus*,
*Macrolepiota procera*), а `world/biome.ts` из плана 2 уже определяет биом
точки по тегам OSM и высоте. Дальше — не данные, а мир:

- [ ] **Дюны как ландшафт**, а не просто тег: сейчас `natural=sand|dune`
      превращается в биом `dunes-coast`, но сама земля рисуется тем же ровным
      мешом, что и лес — ни намёка на песок или маршу.
- [ ] **Интерьер штольни.** Вход берётся из OSM (`cave_entrance`, `adit`,
      `mineshaft`), а подземного пространства за ним пока нет — генерировать
      предстоит самим.
- [ ] **Второй вид дюнного гриба** — *Peziza ammophila* растёт там же, что и
      *Psathyrella ammophila*, но морфологически это чашевидный аскомицет без
      привычной шляпки и ножки — та же проблема схемы, что и с трюфелем ниже,
      только менее радикальная.

## 🍂 Игра

- [ ] **Сезоны и календарь.** Сейчас в лесу вечный сентябрь после дождя.
      Календарь превращает `season` из фильтра в механику: за одну вылазку всех
      видов не собрать, надо возвращаться.
- [ ] **Собственные заметки к находке** — где нашёл, дата, своя пометка. Данные
      для этого в сейве уже лежат (`finds` с координатами и временем).
- [ ] **Экспорт энциклопедии** — картинкой или страницей, чтобы было чем
      похвастаться.

## 🔧 Внутреннее

- [ ] **Подземные грибы (трюфель и родственные) не влезают в схему.** У
      трюфеля нет ни шляпки, ни ножки, ни открытой поверхности со спорами —
      только сплошная мякоть под землёй, а спороносный слой запечатан внутри.
      `morphology: { cap, stipe, hymenium }` рассчитана на надземный гриб с
      этими тремя частями по определению; втиснуть в неё трюфель значило бы
      написать заведомо неверные данные — ровно то, чему посвящён весь проект,
      поэтому он сознательно не добавлен в базу. Нужны: (1) отдельный тип тела
      в схеме и генераторе для гипогейных/гастероидных грибов (тот же класс
      задачи, что и дождевик-Lycoperdon, и вторая дюнная *Peziza ammophila* из
      раздела биомов); (2) отдельная механика находки — трюфель физически
      нельзя увидеть и подобрать прицелом, как обычный гриб, его ищут по
      запаху (в природе — с собакой или свиньёй), значит нужен принципиально
      другой интерфейс поиска, не «подошёл — E».
- [ ] **LOD для грибов.** Сейчас дальние просто выключаются на 45 метрах, а
      должен быть дешёвый силуэт.
- [ ] **Инстансинг грибов одного вида.** 243 меша — 243 вызова отрисовки.
      Экземпляры различаются геометрией, так что понадобится либо несколько
      готовых вариантов на вид, либо атлас.
- [ ] **Тест на регрессию силуэтов.** Осмотр глазами дважды находил то, чего не
      видели тесты (химерные пропорции, боковая ножка). Стоит рендерить эталоны
      в headless и сравнивать.
- [ ] **CHANGELOG.md** — когда релизов станет больше двух.
